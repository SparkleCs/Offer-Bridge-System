<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.offerbridge.backend.mapper.AgencyTeamMemberRelMapper">
  <insert id="insertOne">
    INSERT INTO agency_team_member_rel(team_id, member_id, status)
    VALUES(#{teamId}, #{memberId}, 'ACTIVE')
  </insert>

  <update id="activateRelation">
    UPDATE agency_team_member_rel
    SET status = 'ACTIVE'
    WHERE team_id = #{teamId}
      AND member_id = #{memberId}
  </update>
</mapper>
