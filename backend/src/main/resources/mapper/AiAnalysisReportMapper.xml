<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.offerbridge.backend.mapper.AiAnalysisReportMapper">
  <resultMap id="BaseMap" type="com.offerbridge.backend.entity.AiAnalysisReport">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="report_type" property="reportType"/>
    <result column="input_snapshot_json" property="inputSnapshotJson"/>
    <result column="result_json" property="resultJson"/>
    <result column="model_provider" property="modelProvider"/>
    <result column="model_version" property="modelVersion"/>
    <result column="status" property="status"/>
    <result column="created_at" property="createdAt"/>
  </resultMap>

  <insert id="insertOne" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO ai_analysis_report(
      user_id, report_type, input_snapshot_json, result_json, model_provider, model_version, status
    )
    VALUES(
      #{userId}, #{reportType}, #{inputSnapshotJson}, #{resultJson}, #{modelProvider}, #{modelVersion}, #{status}
    )
  </insert>

  <select id="findLatestByUserId" resultMap="BaseMap">
    SELECT id, user_id, report_type, input_snapshot_json, result_json, model_provider,
           model_version, status, created_at
    FROM ai_analysis_report
    WHERE user_id = #{userId}
      AND report_type = #{reportType}
    ORDER BY created_at DESC, id DESC
    LIMIT 1
  </select>
</mapper>
